# Bundle patch used by DSH plugin installation.

- insert:
    - id: dsh-model-router
      name: "dsh-model-router"
      # Optional non-tier behavior overrides (tier assignments live in DSH settings):
      # config:
      #   classifier:
      #     mode: both
      #   validator:
      #     maxEscalations: 2
      #     stickyScope: turn
