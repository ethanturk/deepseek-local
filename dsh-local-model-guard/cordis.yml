# Bundle patch used by DSH plugin installation.
#
# Load together with the router via dsh-combined-patch.yml at the repo root.

- insert:
    - id: dsh-local-model-guard
      name: "dsh-local-model-guard"
      # Optional config:
      # config:
      #   maxConsecutiveFailures: 2
      #   maxRepeatedCalls: 2
      #   windowSize: 6
      #   enableRetries: true
      #   maxRetries: 1
      #   forceAlways: false
