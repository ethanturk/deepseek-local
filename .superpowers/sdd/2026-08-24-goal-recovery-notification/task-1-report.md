# Task 1 Report: Scaffold the Isolated Plugin Package

## Files

- `dsh-goal-recovery/package.json`
- `dsh-goal-recovery/package-lock.json` (generated by npm)
- `dsh-goal-recovery/tsconfig.json`
- `dsh-goal-recovery/cordis.yml`

The manifest follows the established ESM/test conventions and pins the four
DSH runtime dependencies to `^0.1.0-rc.7`. The TypeScript configuration uses
ES2023, NodeNext, strict mode, `allowImportingTsExtensions`, Node types, and
the requested source/test globs. The Cordis patch is standalone and inserts
the `dsh-goal-recovery` plugin entry.

## Commands and outputs

### `npm install --prefix dsh-goal-recovery`

Passed (exit 0): added 21 packages, audited 22 packages, found 0
vulnerabilities. Generated `dsh-goal-recovery/package-lock.json`.

### `npm run typecheck --prefix dsh-goal-recovery`

Blocked by the intentionally empty source/test globs (exit 2):

```text
error TS18003: No inputs were found in config file
'.../dsh-goal-recovery/tsconfig.json'. Specified 'include' paths were
'["src/**/*.ts","test/**/*.ts"]' and 'exclude' paths were '[]'.
```

This is expected from TypeScript 5.9 when the scaffold has no `.ts` files;
the requested configuration is retained exactly. Once the source files are
added, rerun the same command.

## Self-review

- Scope limited to the requested package manifest, lockfile, TypeScript
  configuration, Cordis patch, and this report.
- No existing plugin package or unrelated worktree files were changed.
- No applicable `STYLE.md` files were present in the worktree.
- Install completed without dependency vulnerabilities.
- Only concern is TS18003 until source or test files exist.
